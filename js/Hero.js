import React from "React";
import ReactDOM from "react-dom";

const Hero = () =>{
    return(
        <div>
        String Name = "Colin Maguire";
        </div>
    );
};

ReactDOM.render(<Hero />, document.getElementById("Hero"));
